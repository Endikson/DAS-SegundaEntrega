<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <!-- Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de inicio de sesión -->
        <activity
            android:name=".LoginActivity"
            android:label="LoginActivity" />

        <!-- Actividad de registro -->
        <activity
            android:name=".RegistroActivity"
            android:label="RegistroActivity" />

        <!-- Actividad de Bienvenida -->
        <activity
            android:name=".WelcomeActivity"
            android:label="WelcomeActivity" />

        <!-- Actividad para ver imágenes -->
        <activity
            android:name=".ViewImagesActivity"
            android:label="ViewImagesActivity" />

        <!-- Actividad para funciones de administrador -->
        <activity
            android:name=".AdminOptionsActivity"
            android:label="AdminOptionsActivity" />

        <!-- Actividad para mostrar imágenes -->
        <activity
            android:name=".ServerImagesActivity"
            android:label="ServerImagesActivity" />

        <!-- Servicio para manejar los eventos de Firebase Messaging -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

    </application>

</manifest>
